server:
  port: 5050

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://47.116.71.124:3306/library?serverTimezone=GMT%2b8
    username: root
    password: root

mybatis:
  mapper-locations: classpath:/mapper/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true     # 数据库下划线转驼峰命名方式，默认是关闭的false

